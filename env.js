// Public, non-secret config for the dashboard frontend.
// Set this to your deployed Render backend URL (no trailing slash).
// Leave blank ("") for local dev, where dashboard.html is served by
// app.py itself on the same origin.
window.EXPENSE_API_BASE = "https://expense-tracker-bot-qed3.onrender.com";
